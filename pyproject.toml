[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "em_filter"
version = "0.1.0"
description = "Emergence network filter agent SDK"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["pynacl>=1.5", "websocket-client>=1.7"]

[project.optional-dependencies]
dev = ["pytest>=8", "websockets>=13"]

[tool.setuptools.packages.find]
where = ["."]
include = ["em_filter*"]
